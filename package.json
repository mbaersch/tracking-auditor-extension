{
  "name": "tracking-auditor-extension",
  "version": "0.7.1",
  "description": "DevTools extension that records GA4, Meta Pixel, Bing UET, TikTok Pixel, Pinterest Tag, Google Ads, LinkedIn Insight Tag, Reddit Pixel and Snapchat Pixel requests of the inspected tab, including custom-loader, base64-encoded and first-party transports.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/package-extension.js",
    "screenshots": "node scripts/generate-screenshots.js"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
