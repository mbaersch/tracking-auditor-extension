{
  "name": "tracking-auditor-extension",
  "version": "0.4.0",
  "description": "DevTools extension that records GA4, Meta Pixel, Bing UET, TikTok Pixel and Pinterest Tag requests of the inspected tab, including custom-loader, base64-encoded and first-party transports.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/package-extension.js"
  }
}
