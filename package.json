{
  "name": "tracking-auditor-extension",
  "version": "0.2.0",
  "description": "DevTools extension that records GA4 and Meta Pixel requests of the inspected tab, including custom-loader, base64-encoded and first-party transports.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/package-extension.js"
  }
}
