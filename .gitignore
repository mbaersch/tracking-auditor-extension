node_modules/
dist/
*.zip
test-results/
playwright-report/
tmp-*/
.DS_Store
*.stackdump
AGENTS.md
.claude/

# Captured traffic / session exports used only as fixture sources — never commit
# (large, may carry PII). The relevant hits are baked into tests/*.test.js.
*.har
tag-assistant-*.json

# Local dev helpers for console-firing hard-to-trigger vendor events (used to
# capture fixtures, e.g. Criteo window.criteo_q). Kept locally, not shipped.
console-snippets/
*-console-snippet.js

